   var AWS = require('fs');
    //const ddb = new AWS.DynamoDB({apiVersion: '2012-10-08'});
    

    var res ={
        "statusCode": 200,
        "headers": {
            "Content-Type": "*/*"
        }
    };

    res.body = "GET: "+(!!AWS); //+JSON.stringify(context);
    callback(null, res);
};
